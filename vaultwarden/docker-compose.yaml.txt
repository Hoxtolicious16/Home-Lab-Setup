services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    ports:
      - "80:80"
    volumes:
      - ./data:/data
    environment:
    - ADMIN_TOKEN=___CHANGE_THIS_FOR_ADMIN_PANEL___
    restart: unless-stopped